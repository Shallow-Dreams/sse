#include  <stdio.h>
#define N 40
#include <string.h>
int main()
{
	int i,b,c;
	int a[10];
	printf("Enter %d numbers:",10);
	for(i=0;i<10;i++)
	{
		scanf("%d",&a[i]);
	}
	printf("In reverse order:");
	for(i=9;i>=0;i--)
	{
		printf(" %d",a[i]);
	}
	
    return 0;
}
