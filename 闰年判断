#include <stdio.h>
#include <string.h>
#include <math.h> 
#include <stdio.h>
#include <string.h>
#include <math.h> 
int main()
{
  	int x,y;
  	double area,s;
  	printf("输入年份:");
  	scanf("%d",&x);
  	

	if((x%4==0)&&(x%100!=0)||(x%400==0))
	  {
	  
	  		printf("%d是闰年!\n",x);	
	  }
	  else 
	  {
	  	printf("%d不是闰年!\n",x);
	  }
	  
	  
    return 0;
}
